import { TransformCodePipe } from './transform-code.pipe';

describe('TransformCodePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCodePipe();
    expect(pipe).toBeTruthy();
  });
});
